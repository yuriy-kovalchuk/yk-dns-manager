# Config file paths (defaults to configs/*.yaml if unset)
DOMAIN_MAP_PATH=configs/domain-map.yaml
DNS_PROVIDER_PATH=configs/dns-provider.yaml

# OPNsense credentials (referenced as ${ENV_VAR} in dns-provider.yaml)
OPNSENSE_API_KEY=
OPNSENSE_API_SECRET=
